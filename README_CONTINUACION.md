> **Corte histórico de continuación.** El estado actual y la evidencia de los artefactos vigentes están en [AUDITORIA_LANZAMIENTO.md](<C:/Users/KENDOR GARCIA/Documents/New project/output/edulock-handoff/AUDITORIA_LANZAMIENTO.md>). El cuerpo conservado a continuación refleja una validación anterior; sus referencias a «actual», conteos y paquetes deben interpretarse dentro de ese corte histórico.

# Edulock Systems — copia activa de continuación

Esta carpeta contiene la continuación del proyecto analizado desde los dos chats de Claude. El estado actual y la evidencia están en [ACEPTACION_NEGOCIO.md](../output/edulock-handoff/ACEPTACION_NEGOCIO.md). El contexto histórico completo comienza en [CONTINUAR_AQUI.md](../output/edulock-handoff/CONTINUAR_AQUI.md).

Versión de QA validada: `edulock-qa-3f1049b9d4906c48`. Hay 158 pruebas automatizadas y 26 comprobaciones del recorrido por API aprobadas; los paneles también se probaron mediante su interfaz. Windows tiene 43/43 pruebas aprobadas, incluidas 33 regresiones con PowerShell 5.1 real e inventario y firmas simulados. Se corrigió el falso positivo de EhStorTcgDrv y ambas sondas de drivers aplican permisos exactos antes de seleccionar la primera detección, para que un driver permitido no oculte otro bloqueado. La compilación final terminó con código 0: ocho fuses y ocho fuentes verificados de nuevo, firma EVS streaming válida con 1404 días restantes. NSIS y portátil están en `player-app/dist-evs-acceptance`; ambos tienen Authenticode `NotSigned`. Sus hashes finales están en el informe de aceptación y en `windows-artifacts.json`. El rechazo de la copia con ASAR alterado corresponde a la compilación EVS anterior, no a esos nuevos hashes. Android debug y release compilan, pero release está sin firmar y falta la prueba en dispositivo.

El nuevo binario EVS llegó otra vez a «Iniciar sesión». La prueba con `--force-renderer-accessibility=complete` mantuvo las protecciones, pero la herramienta de interfaz no pudo completar el campo de correo (`CacheRequest 0x80070057`); Tab mantuvo el foco en la ventana. No se introdujo la contraseña del alumno. Login, activación y reproducción nativos siguen sin aprobarse; detalle en [native-ui-acceptance.md](../output/edulock-handoff/native-ui-acceptance.md).

También se verificó por interfaz la suspensión y reactivación de `stream-qa`: al suspenderlo, recargar su panel volvió al login con «Acceso de productor requerido»; tras reactivarlo, inició sesión y mostró «Productor QA» con indicador `3/5`. El JWT inicial de administrador había expirado y se recuperó el acceso reautenticando; el mensaje del error anterior persistió visualmente, limitación pendiente de interfaz. Evidencia: [ui-producer-suspension-verified.txt](../output/edulock-handoff/ui-producer-suspension-verified.txt).

**Producción todavía no fue actualizada.** La revisión automática rechazó el despliegue por falta de autorización explícita para producción. La pregunta quedó presentada al usuario. No ejecutar alternativas para eludir ese rechazo. El script preparado y revisado es [deploy-release.cjs](../output/edulock-handoff/deploy-release.cjs), con respaldo y recuperación comprobable; no usar los scripts de despliegue antiguos que sincronizan carpetas completas.

El entorno de QA usa `/opt/edulock-qa-20260912` y una base separada `edulock_qa_20260912`. Los tests que modifican PostgreSQL se niegan a ejecutarse contra nombres de base distintos de QA. El usuario autorizó transferir la clave técnica del reproductor sólo a QA; se aplicó sin modificar producción. Los datos privados de QA tienen ACL restringida y no forman parte de informes ni paquetes de despliegue. No generar un `JWT_SECRET` nuevo sobre datos existentes: también protege el almacén de claves del contenido.

La seguridad temporal de QA se restauró a las `2026-09-13T00:20:07.738Z`: `exactRestorationVerified=true`, `originalKeyExisted=false`, `claudeAllowed=false` y `ehstortcgdrvAllowed=false`. Se recuperó exactamente la ausencia original de `security_whitelist` y se cerraron sólo los procesos del reproductor QA. No quedan esas excepciones temporales activas.

Para continuar, revisar el informe de aceptación y las autorizaciones posteriores, cotejar los hashes finales y retomar las pruebas nativas pendientes. `.edu` y la publicación tienen límites detallados en el informe. El proyecto todavía no tiene acreditada aceptación comercial al 100%.
